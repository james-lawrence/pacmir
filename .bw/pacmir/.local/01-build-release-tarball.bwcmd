- command: set -o allexport; source %bwroot/bw.env; set +o allexport; gh release delete -y v${PACMIR_RELEASE_VERSION}
  lenient: true # best effort to delete a release, the CLI returns an error when the release doesn't exist. it'll fail on the next command if the release actually failed to be deleted.
- command: set -o allexport; source %bwroot/bw.env; set +o allexport; gh release create v${PACMIR_RELEASE_VERSION} --target ${PACMIR_GIT_COMMIT_CHECKSUM} --title "pacmir v${PACMIR_RELEASE_VERSION}" --repo james-lawrence/pacmir -F %bwroot/changelog.md 